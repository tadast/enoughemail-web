.container.mx-auto.px-6
  %p#notice= notice

  %h1.text-4xl.my-6= @filter_rule_template.name

  .space-y-6
    %p= @filter_rule_template.description

    %p
      %b Filtered Emails:
      %pre.p-4.bg-gray-100.overflow-y-auto.max-h-96= @filter_rule_template.email_pattern.gsub(" OR ", "\n")

    - if @filter_rule_template.email_pattern_as_array_for_gmail.size > 1
      %p.small.italic
        = inline_svg_tag "fa-icons/solid/info.svg", class: "h-5 w-5 p-1 inline-block border rounded-full"
        This template will be applied as
        = pluralize(@filter_rule_template.email_pattern_as_array_for_gmail.size, "filter rule")
        %span>. This is because Gmail has a character limit per filter and this list is above that limit.
    .flex.space-x-5
      = link_to 'Apply', filter_rule_templates_path, class: "inline-block px-7 py-3 mb-2 md:mb-0 mr-0 md:mr-2 bg-indigo-600 text-white font-medium text-sm leading-snug uppercase rounded shadow-md hover:bg-indigo-700 hover:shadow-lg focus:bg-indigo-700 focus:shadow-lg focus:outline-none focus:ring-0 active:bg-indigo-800 active:shadow-lg transition duration-150 ease-in-out"
      = link_to 'Back', filter_rule_templates_path, class: "inline-block px-7 py-3 mb-2 md:mb-0 mr-0 md:mr-2 border border-indigo-700 font-medium text-sm leading-snug uppercase rounded shadow-md hover:shadow-lg focus:shadow-lg focus:outline-none focus:ring-0 active:shadow-lg transition duration-150 ease-in-out"
